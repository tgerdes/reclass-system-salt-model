applications:
- keepalived
classes:
- service.keepalived.support
parameters:
  keepalived:
    cluster:
      enabled: true
      instance:
        radosgw_mon_public_vip:
          address: ${_param:keepalived_radosgw_mon_host_public}
          password: ${_param:keepalived_radosgw_mon_host_public}
          interface: ${_param:keepalived_radosgw_mon_vip_interface_public}
          virtual_router_id: 233
          priority: ${_param:keepalived_vip_priority}

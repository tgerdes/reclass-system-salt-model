parameters:
  mysql:
    client:
      server:
        database:
          database:
            ironic:
              encoding: utf8
              users:
              - name: ironic
                password: ${_param:mysql_ironic_password}
                host: '%'
                rights: all
              - name: ironic
                password: ${_param:mysql_ironic_password}
                host: ${_param:single_address}
                rights: all
